# Local ledger / simulation artifacts
*.db
*.sqlite
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Secrets — must never be committed (keys are out-of-band only)
.env
*.key
*.pem
secrets/
